1. **FizzBuzz Classic** — генерує масив від 1 до `n` із заміною чисел на "Fizz", "Buzz" або "FizzBuzz".
2. **Number Sign** — визначає знак числа (`1`, `-1`, `0` або `NaN`).
3. **Leap Year Checker** — перевірка року на високосність.
4. **Triangle Type** — класифікація трикутника за сторонами (`equilateral`, `isosceles`, `scalene` або `invalid`).
5. **Grade Calculator** — конвертація балів (0-100) у літери (A-F) з модифікаторами `+` та `-`.
